var path = require('path'),
    share = require('share'),
    config = share.config,
    log = share.log,
    app = share.app,
    request = require('supertest');

describe('GET /admin/video/meta/:videoId', function() {
    // it('should respond 200, application/json', function(done) {
        // this.timeout(5000);
        // request(app)
        // .get('/admin/video/meta/1541241')
        // .set('Accept', 'application/json')
        // .expect(200)
        // .end(function(error, res) {
            // if (error) return done(error);
            // else return done();
        // });
    // });
});

